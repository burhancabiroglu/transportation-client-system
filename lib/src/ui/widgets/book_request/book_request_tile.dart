import 'package:flutter/material.dart';

class BookRequestTile extends StatelessWidget {
  const BookRequestTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}